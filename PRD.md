# 手机周期闹钟 App 产品需求文档

## 1. 产品定位

手机周期闹钟是一款面向 Android 用户的离线周期提醒工具。第一版优先服务中国安卓市场用户，重点兼容 Android 5.0 及以上旧手机、低端手机和国产 ROM，解决普通系统闹钟不擅长的“非标准周期提醒”问题。

首发形态为 Android App。完整产品规划包含图片中的 6 项主要功能：

1. 每 N 天闹钟
2. 每 N 小时闹钟
3. 每月 X 号闹钟
4. 农历日期闹钟
5. 法定假日调休闹钟
6. 地点触发闹钟

当前实现优先级已经收紧：先接管 Qclaw 源码，把“每 N 天闹钟”做成完整可运行闭环。其余 5 项先保留规划和入口，不在第一轮实现。

第一版不做账号、云同步、广告、社交、服务端推送和 iOS 版本。

## 2. 目标用户

- 使用旧手机或低端手机的用户，需要稳定、轻量、离线可用的提醒工具。
- 倒班、隔天工作、周期服药、周期护理、周期缴费、周期检查设备的人。
- 需要农历生日、祭祀、节日、法定假日调休提醒的人。
- 需要到达或离开某个地点后提醒的人，但地点触发在 MVP 中作为后置能力。

## 3. 成功标准

- Android 5.0 及以上设备可以安装、打开、创建和触发基础闹钟。
- 无账号、无网络状态下，前 5 项周期闹钟可完整使用。
- 用户能在 30 秒内创建一个“每 N 天闹钟”或“每月 X 号闹钟”。
- 锁屏、App 不在前台、设备重启后，已启用闹钟仍能重新注册并尽力触发。
- 权限申请只在用户触发相关功能时出现，并说明用途。
- 首版能准备中国安卓市场所需的隐私政策、权限说明、截图和审核备注。

## 4. 当前第一轮 MVP

第一轮 MVP 只实现“每 N 天闹钟”。

必须完成：

- 创建每 N 天闹钟。
- 编辑每 N 天闹钟。
- 删除每 N 天闹钟。
- 启用和停用每 N 天闹钟。
- 列表显示下次响铃时间。
- 到点触发提醒。
- 关闭和贪睡。
- App 后台时触发。
- 锁屏时尽力触发。
- 手机重启后重新注册已启用闹钟。
- 系统时间或时区变化后重新计算。

其余 5 项处理方式：

- 每 N 小时闹钟：保留入口，可显示“后续支持”。
- 每月 X 号闹钟：保留入口，可显示“后续支持”。
- 农历日期闹钟：保留入口，可显示“后续支持”。
- 法定假日调休闹钟：保留入口，可显示“后续支持”。
- 地点触发闹钟：后置，不在第一轮申请定位权限。

## 5. 完整规划功能

### 4.1 公共闹钟能力

每个闹钟规则都包含：

- 名称
- 启用状态
- 触发时间
- 重复规则
- 铃声
- 震动开关
- 贪睡设置
- 备注
- 下次提醒时间
- 创建时间和更新时间

闹钟列表展示：

- 闹钟名称
- 规则摘要
- 下次响铃时间
- 开关
- 编辑入口
- 删除入口

闹钟触发页要求：

- 锁屏可见或高优先级提醒
- 显示闹钟名称、规则摘要、当前时间
- 提供关闭和贪睡
- 不展示广告
- 不要求登录

### 4.2 每 N 天闹钟

用途：隔天、每 3 天、每 10 天等固定天数周期提醒。

字段：

- 起始日期
- 每隔 N 天，N 范围为 1 到 999
- 每天触发时间
- 名称、铃声、震动、贪睡、备注

规则：

- 从起始日期开始计算周期。
- 如果起始日期早于今天，按 N 天周期计算下一个大于当前时间的触发点。
- 用户修改系统时间或时区后，重新计算下次触发时间。

免费版包含该功能。

### 4.3 每 N 小时闹钟

用途：每 2 小时喝水、每 6 小时服药、周期检查等。

字段：

- 起始日期时间
- 每隔 N 小时，N 范围为 1 到 999
- 可选每日有效时间段，例如 08:00 到 22:00
- 名称、铃声、震动、贪睡、备注

规则：

- 从起始日期时间开始计算周期。
- 如果设置每日有效时间段，触发点落在时间段外时跳到下一次有效时间段内的周期点。
- 对短间隔提醒要避免长期前台服务常驻，优先注册下一次系统闹钟，触发后再注册下一次。

免费版包含基础能力；Pro 可开放更复杂的有效时间段。

### 4.4 每月 X 号闹钟

用途：每月还款、缴费、纪念日提醒。

字段：

- 每月日期 X，范围为 1 到 31
- 触发时间
- 名称、铃声、震动、贪睡、备注

规则：

- 当月存在 X 号时，在该日触发。
- 当月不存在 X 号时，默认跳过到下一个存在 X 号的月份。
- 例如每月 31 号：2 月、4 月、6 月、9 月、11 月不触发。

免费版包含该功能。

### 4.5 农历日期闹钟

用途：农历生日、传统节日、纪念日提醒。

字段：

- 农历月份
- 农历日期
- 是否只在闰月触发
- 是否包含闰月同日
- 触发时间
- 名称、铃声、震动、贪睡、备注

规则：

- 使用本地农历换算库或内置农历数据，不依赖网络。
- 下次触发时间按未来公历日期计算。
- 闰月策略必须可见，默认“不包含闰月同日”。

免费版可支持常规农历日期；Pro 可开放高级闰月策略。

### 4.6 法定假日调休闹钟

用途：只在工作日响、调休日响、节假日不响、节假日前提醒等。

字段：

- 规则类型：工作日响、休息日响、节假日前一天提醒、节假日当天提醒
- 触发时间
- 年度假日数据版本
- 名称、铃声、震动、贪睡、备注

规则：

- 第一版使用内置中国法定节假日和调休 JSON。
- 每年通过 App 更新维护新年度数据。
- 列表和编辑页显示当前使用的数据年份，避免用户误解。
- 如果下一年数据还未内置，提示用户更新 App 或手动确认。

免费版可支持当年数据；Pro 可开放多规则组合。

### 4.7 地点触发闹钟

用途：到公司提醒打卡、到家提醒事项、离开某地提醒。

字段：

- 地点名称
- 经纬度
- 半径
- 触发类型：进入、离开
- 有效时间段
- 名称、铃声、震动、贪睡、备注

规则：

- 该功能后置开发，不能阻塞前 5 项 MVP 上线。
- 只在用户创建地点闹钟时申请定位权限。
- 优先使用系统地理围栏能力。
- 用户拒绝定位权限时，不影响其他闹钟功能。
- 隐私政策必须清楚说明位置数据用途；第一版默认位置数据只保存在本机。

地点触发建议作为 Pro 或实验功能。

## 6. 免费版和 Pro 边界

免费版：

- 每 N 天闹钟
- 每 N 小时基础闹钟
- 每月 X 号闹钟
- 常规农历日期闹钟
- 当年法定假日调休基础提醒
- 基础铃声、震动、贪睡、删除、编辑

Pro 候选：

- 地点触发闹钟
- 更多闹钟数量
- 批量管理
- 高级有效时间段
- 高级农历闰月策略
- 多年度假日数据
- 本地备份导入导出
- 高级铃声和主题

第一版不接广告 SDK。后续如接入付费 SDK 或统计 SDK，必须更新隐私政策和商店数据声明。

## 7. 低端旧机体验要求

- 最低支持 Android 5.0 / API 21。
- 首页应轻量、启动快、无登录墙。
- UI 避免大量动画、复杂渐变、重型依赖和大图背景。
- 基础闹钟功能不依赖网络。
- 权限申请采用渐进式：通知、精准闹钟、全屏提醒、定位分别在需要时申请。
- 对国产 ROM 的省电/后台限制，提供明确的设置引导，但不强制用户操作。

## 8. 非目标功能

- 不做 Android 5.0 兼容，除非后续确认目标用户中占比很高。
- 不做 iOS 首发。
- 不做账号体系。
- 不做云同步。
- 不做广告变现。
- 不做服务端推送。
- 不做社交、社区、用户内容发布。

## 9. Qclaw 原型接管要求

当前工作区尚未包含 Qclaw 原型代码。接管时需要提供 Qclaw 项目的绝对路径或把项目复制到当前仓库。

接管检查项：

- 技术栈：Android 原生、Flutter、React Native、uni-app 或其他。
- 构建命令和依赖版本。
- 是否已经实现每 N 天闹钟。
- 是否已有本地存储。
- 是否已有系统闹钟注册逻辑。
- 是否已有通知、锁屏、重启恢复能力。
- 是否已有权限申请逻辑。
- 哪些代码可保留，哪些需要重构。
