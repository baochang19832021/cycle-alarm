# 首页功能目录开发

## 时间
2026-06-21 19:19

## 需求
在首页（AlarmListActivity）做一个功能目录界面，展示4~5星功能，点击跳转对应子页面

## 实现

### 新增文件
- `item_function_card.xml` — 功能卡片布局
  - 卡片圆角16dp、白底、阴影2dp
  - 三个TextView：功能名称(14sp粗体)、星级(★★★★★ 橙色)、状态标签(12sp)

### 修改文件
- `activity_alarm_list.xml` — 在标题和闹钟列表之间插入：
  - "功能目录" 分区标题（24dp padding，16sp粗体）
  - `rvFunctionGrid` RecyclerView（2列网格，wrap_content）
  - 分割线（1dp，#FFEEEEEE）
  - "我的闹钟" 分区标题

- `AlarmListActivity.kt`：
  - 新增 import：GridLayoutManager
  - 新增 `rvFunctionGrid` 成员变量
  - onCreate 中初始化 rvFunctionGrid + 调用 loadFunctionGrid()
  - 新增 `loadFunctionGrid()` 方法：构建6个功能项，设置 adapter
  - 新增 `FunctionItem` data class（name, stars, isImplemented）
  - 新增 `FunctionAdapter`（2列网格适配器，onClick 回调）

### 功能目录内容（6项）
| 功能 | 星级 | 状态 |
|------|------|------|
| 间隔天数 | ★★★★★ | 已支持（跳转MainActivity） |
| 每月提醒 | ★★★★★ | 开发中 |
| 农历日期 | ★★★★★ | 开发中 |
| 多次提醒 | ★★★★☆ | 开发中 |
| 节假日适配 | ★★★★☆ | 开发中 |
| 完成打卡 | ★★★★☆ | 开发中 |

## 下一步
用户选择一个功能开始实现（建议优先：每月提醒 / 农历日期）
