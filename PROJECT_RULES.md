# hospital-alarm 项目开发规范

> ⚠️ 每次 hospital-alarm 项目有代码改动前，AI必须先过一遍此清单

## 🔴 绝对禁止的错误

### 错误1：改布局ID后代码还在引用旧ID
**检查规则**：
- 删除/重命名任何布局ID → 立刻在项目中全局搜索这个ID
- 确认 Kotlin 代码里没有 `findViewById(R.id.xxx)` 或 `R.id.xxx` 引用
- 确认 XML 布局里没有 `@+id/xxx` 引用

### 错误2：改完代码不自查直接推给用户
**检查规则**：
- 每次改完，按下方「改动后自查清单」逐项确认
- 自己构建检查（或心里模拟构建），确认没有明显编译错误

### 错误3：UI调整需要多轮往返
**检查规则**：
- 改UI前先说清楚：改哪几个文件、每个文件改哪几行、预期效果是什么
- 改完后自己判断：这像小米闹钟的风格吗？字号/间距/颜色协调吗？

---

## ✅ 改动后自查清单

通用规则：**改了任何东西 → 检查所有依赖它的地方**

### ID/引用层面
- [ ] 删除/重命名布局ID → 全局搜索确认 Kotlin/XML 里无引用
- [ ] 新增ID → Kotlin 代码里绑定了？
- [ ] 修改 strings.xml → 检查有没有地方硬编码了旧文字
- [ ] 修改颜色/尺寸常量 → 检查所有引用处

### 类/文件层面
- [ ] 重命名/删除 .kt 文件 → 检查 Manifest、import、所有调用处
- [ ] 改了 WheelView 构造参数 → 所有 `WheelView(...)` 调用处同步
- [ ] 改了 AlarmScheduler 接口 → AlarmListActivity/MainActivity/Receiver/Service 全要检查

### 功能层面
- [ ] 删了一个功能 → 权限声明、布局、广播接收器都删干净了吗？
- [ ] 改了数据模型字段 → JSON序列化、UI绑定、调度器都更新了吗？
- [ ] 新增权限 → Manifest 声明了？运行时请求了？

### 空安全
- [ ] 可能为空的对象（findViewById、intent extras、文件读取）→ 加空判断

### UI层面
- [ ] 状态栏会不会挡住？（MIUI 状态栏更高）
- [ ] 文字大小合适吗？间距舒服吗？像小米闹钟风格吗？

### 构建
- [ ] 构建一下，有没有编译错误？

---

## 📐 hospital-alarm 设计规范

- **配色**：主色 `#FF7043` 橘橙，关闭状态 `#BBBBBB` 灰，开启状态 `#1A1A1A` 黑
- **字体**：标题 30sp 粗体，时间 36sp 粗体，正文 16sp
- **圆角**：卡片 24dp，按钮 22dp（或 50% 圆形）
- **间距**：标题 paddingTop 48dp，卡片间距 16dp
- **滚轮**：选中 38sp 粗体 #222222，未选 28sp #999999，itemHeight=52dp，visibleCount=3

---

## 📝 已踩过的坑

| 日期 | 错误 | 根因 | 修复 |
|------|------|------|------|
| 06-21 | `Unresolved reference: pickerDays` | 删除布局ID后没改代码引用 | 全局搜索ID后删除代码引用 |
| 06-20 | 状态栏遮挡标题 | paddingTop 32dp 不够 MIUI 用 | 改 48dp + 重写布局结构 |
| 06-20 | 图标变实心圆 | 两个 path 用 fillColor 叠加 | 改 stroke + transparent fill |
| 06-20 | NumberPicker 反射失效 | Android 12+ 限制反射 | 自绘 WheelView |

---

*这个文件会在每次改动前被加载，强制AI过一遍检查清单*
