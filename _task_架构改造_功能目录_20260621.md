# 架构改造：主页改功能目录 + MainActivity双模式

## 改造时间
2026-06-21 19:42

## 用户需求
1. **主页（AlarmListActivity）** = 只显示功能目录（无闹钟列表、无FAB）
2. **间隔天数子页面** = 点击"间隔天数"后进入，显示该类型的所有闹钟卡片
3. **编辑页面** = 点击闹钟卡片后进入编辑表单
4. 其他未实现功能卡片 → Toast"开发中"

## 改造方案

### 模式定义
- **VIEW 模式**：浏览某个功能类型的所有闹钟列表
  - Intent: `action = AlarmListActivity.ACTION_VIEW`, `EXTRA_TYPE = "interval"`
  - 标题：功能名（如"间隔天数(1~365天)"）
  - 闹钟卡片列表 + 橙色FAB
  - FAB点击 → 切换到 EDIT 模式（新建）

- **EDIT 模式**：原有表单逻辑
  - Intent: 默认（无 ACTION_VIEW）
  - 标题："新建闹钟" 或 "编辑闹钟"
  - 表单编辑界面

### 文件改动

#### 1. activity_alarm_list.xml
- 删除了：闹钟列表 RecyclerView、Empty state、FAB、"我的闹钟"标题分割线
- 保留了：标题 + 功能目录 RecyclerView

#### 2. AlarmListActivity.kt
- 删除：loadAlarms()、AlarmAdapter、rvAlarmList、layoutEmpty、FAB、onResume()
- 改为：点击"间隔天数"→ `Intent(this, MainActivity::class.java).apply { action=ACTION_VIEW; putExtra(EXTRA_TYPE, TYPE_INTERVAL) }`
- 其他功能卡片 → Toast"开发中"

#### 3. activity_main.xml
- 新增 `layoutViewMode` LinearLayout：
  - 标题栏（btnBack + "间隔天数(1~365天)"）
  - "我的闹钟" 标题
  - `rvAlarmList` RecyclerView
- 新增 `fabAddAlarm` FAB（VIEW模式专用）
- 原有ScrollView → `layoutEditMode`
- 两个布局互斥显示

#### 4. MainActivity.kt
- 新增 `SimpleAlarmItem` + `SimpleAlarmAdapter`（VIEW模式专用）
- `setupViewMode()`：加载并显示闹钟列表，FAB切换到EDIT
- `switchToEditMode(alarmId)`：VIEW→EDIT切换
- `setupEditMode()`：原有表单逻辑
- `rebuildEditMode()`：重构出的可重入方法（切换模式后重新初始化表单）
- 原有所有功能（轮盘、日期、间隔、铃声、备注、震动）保持不变

### 导航流程
```
AlarmListActivity (主页)
  └── 点击"间隔天数(1~365天)"
        └── MainActivity (VIEW模式) ──显示闹钟列表
              ├── 点击闹钟卡片 → MainActivity (EDIT模式, editAlarmId=xxx) ──编辑表单
              └── 点击FAB    → MainActivity (EDIT模式, editAlarmId=null) ──新建表单
```

### 后续开发约定
- 新增功能（如"每月X号"）→ 在 AlarmListActivity.loadFunctionGrid() 添加 TYPE_MONTHLY case
- 对应编辑页面 → MainActivity setupEditMode() 中处理 TYPE_MONTHLY 的特殊字段
