# hospital-alarm 开发错误模式总结

## 反复犯的低级错误

### 错误模式1：改布局忘改代码引用
- **今天**：`dialog_interval_picker.xml` 删掉 `pickerDays`，但 `IntervalPickerDialog.kt` 第38行还在 `removeView(findViewById(R.id.pickerDays))`
- **昨天**：多次修改 padding/margin 后，Kotlin 代码里还有硬编码的布局参数引用
- **根因**：没有建立"改XML → 立刻全局搜索这个ID是否还在代码里被引用"的习惯
- **修复规则**：**每次删除或重命名布局ID，必须立刻在项目中搜索这个ID，把所有引用一并改掉**

### 错误模式2：改完不自查
- 写完代码直接给用户，等用户截图报错才发现
- **根因**：没有改完后的自查清单
- **修复规则**：见下方自查清单

### 错误模式3：UI调整需要多轮
- 状态栏遮挡：paddingTop 32dp → 64dp → 重写布局（3轮）
- FAB颜色和形状：蓝色方形 → 橙色圆形（2轮）
- 标题位置：用户说没变化，才发现改错文件或没生效（多次）
- **根因**：我没有在改之前先在脑中完整想一遍"这个改动会影响哪些文件、哪些像素位置"，而是改一点推一点
- **修复规则**：**UI改动前先画（或写）清楚改哪些文件、每个文件改哪几行，确认逻辑自洽后再动手**

---

## 自查清单（每次改完代码必须过一遍）

- [ ] 布局XML里删除/重命名的ID，在Kotlin代码里还有引用吗？（全局搜索）
- [ ] 新增的ID，在Kotlin代码里绑定了吗？
- [ ] 改了某个文件，其他关联文件需要同步改吗？（布局↔Kotlin，values↔布局）
- [ ] 有可能会出现空指针的地方（findViewById、intent extras、文件读取），加了吗？
- [ ] UI改动：状态栏挡住了吗？文字会不会太小或太大？间距看起来舒服吗？
- [ ] 构建一下（或在大脑里模拟构建），有没有明显的编译错误？

---

## 工作模式承诺

1. **先设计再写代码**：改UI前，先在回复里说清楚"改A文件的第X行，改B文件的第Y行"，让用户确认思路对不对，再动手
2. **改完必自查**：用上方清单过一遍，确认没有低级错误再推给用户
3. **主动审美**：每次调整UI，自己先判断"这个看起来像小米闹钟吗？协调吗？"，不等用户说"太丑了"才改
4. **减少确认往返**：能自己判断的对齐、间距、字号，直接做好，不要每调一个dp都问用户

---

*最后更新：2026-06-21 18:15*
