# 闪退修复 - 间隔天数滚轮空指针崩溃

## 时间
2026-06-21 18:33

## 问题
用户滑动0~365间隔天数滚轮时闪退，手机提示"周期闹钟因自身空指针问题出现多次闪退"

## 根因
`IntervalPickerDialog.kt` 的 `onIndexChanged` 回调里每次都调用 `findViewById(R.id.tvUnitHint)`：
- 滑动时 WheelView 高频触发 onIndexChanged（每帧动画都可能触发）
- 快速滑动 + Dialog 状态变化时，findViewById 可能返回 null
- 对 null 调用 .text → NullPointerException → 崩溃

## 修复
1. 在 onCreate 时缓存 tvUnitHint 引用到成员变量（private var tvUnitHint: TextView? = null）
2. onIndexChanged 回调里改用 tvUnitHint?.text（安全调用）
3. 初始赋值也改用缓存引用

## 改动文件
- `app/src/main/java/com/hospital/alarm/IntervalPickerDialog.kt`

## 教训（加入 _MISTAKES.md）
- 回调函数里不要反复 findViewById，应在初始化时缓存引用
- 高频触发的回调必须做空安全处理（?. 操作符）
- 这是 _MISTAKES.md 第14条错误
