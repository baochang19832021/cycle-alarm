# hospital-alarm 项目 - 完整错误账本

> 从第一天到现在，所有犯过的错误全部记录在此。
> 每次开始新功能前，先读一遍这个文件。

---

## 一、布局 / UI 错误

### 1. 状态栏遮挡标题
- **何时**：2026-06-20，开发第一天
- **错误**：标题 paddingTop 32dp，MIUI 状态栏更高，完全遮挡
- **花了多少轮**：3轮（32dp → 64dp → 重写整个布局结构）
- **根因**：没有提前了解 MIUI 状态栏高度差异
- **正确做法**：一开始就用 48dp 或 `fitsSystemWindows`

### 2. FAB 颜色和形状
- **何时**：2026-06-20
- **错误**：蓝色方形，用户要橘色圆形，改了2轮
- **根因**：没有一开始就按小米风格设计颜色
- **正确做法**：开局就确认主色调和圆角规范

### 3. 标题位置
- **何时**：2026-06-20
- **错误**：RecyclerView 从下往上延伸，覆盖标题，花了多轮调试
- **根因**：FrameLayout 里用 weight=1 的 RecyclerView 会撑满所有剩余空间，没有预留明确间距
- **正确做法**：RelativeLayout 或给标题加明确的 marginBottom

### 4. 图标变实心圆
- **何时**：2026-06-20
- **错误**：矢量图标的两个 path 都用 fillColor，Android 合并渲染成实心圆
- **花了多少轮**：2轮（缩小半径 → 改 stroke + transparent fill）
- **根因**：不知道 VectorDrawable 多 path 渲染规则
- **正确做法**：外圈用 stroke + transparent fill

### 5. NumberPicker 在 Android 12+ 失效
- **何时**：2026-06-20
- **错误**：用反射自定义 NumberPicker，Android 12 限制了反射，不工作
- **花了多少轮**：5轮（反射 → 自绘 Canvas → Bigkoo 库引入失败 → 回到自绘 → 单一 View 方案）
- **根因**：Android 12 变更了 NumberPicker 内部实现，没有提前测试
- **正确做法**：测试环境覆盖各 Android 版本；优先用官方 API

### 6. 间隔天数滚轮与时间滚轮样式不一致
- **何时**：2026-06-21
- **错误**：编辑页时间滚轮用自绘 WheelView（字号大、有缩放渐变），间隔天数弹窗用系统 NumberPicker（字号小、无缩放）
- **根因**：两个功能是不同时间加的，没有统一视觉规范
- **正确做法**：一开始就建立设计规范文档，所有选择器统一参数

### 7. 删除布局 ID 后代码引用未清理
- **何时**：2026-06-21
- **错误**：删了 `dialog_interval_picker.xml` 里的 `pickerDays` ID，但 `IntervalPickerDialog.kt` 第38行还在 `removeView(findViewById(R.id.pickerDays))`
- **根因**：改完 A 文件后没有同步检查所有引用 A 的地方
- **正确做法**：删除/重命名任何 ID，立刻全局搜索所有引用

### 8. UI 调整需要多轮往返
- **何时**：贯穿整个开发过程
- **错误**：每改一个 dp 就问用户对不对，改完再推，等反馈再改
- **根因**：没有在动手前先想清楚设计意图，没有自己判断"这看起来对不对"
- **正确做法**：先设计再写代码，改完自己过一遍"像小米风格吗"，不要每步都问

---

## 二、开发流程错误

### 9. 没有先设计再动手
- **何时**：贯穿全程
- **错误**：拿到需求就直接改代码，改一点推一点
- **根因**：急着出结果，省略了设计环节
- **正确做法**：改之前先说清楚"改哪几个文件、改哪几行、预期效果是什么"

### 10. Bigkoo PickerView 依赖白加
- **何时**：2026-06-20
- **错误**：引入了不存在的依赖，Sync 失败，浪费时间
- **根因**：没有先验证依赖可用性
- **正确做法**：任何第三方库，先查证可用性再引入

### 11. 没有跨文件交叉检查的习惯
- **何时**：贯穿全程
- **错误**：改 Kotlin 文件后不检查关联的 XML；改 XML 后不检查 Kotlin 里的引用
- **根因**：每次只盯着一个文件看
- **正确做法**：改任何文件时，同步打开所有关联文件一起过

### 12. 不敢自己做决定
- **何时**：贯穿全程
- **错误**：颜色差一度、间距差几个 dp 都要问用户
- **根因**：怕做错、不确定什么是对的
- **正确做法**：参考小米闹钟截图，自己判断，不要把决策责任推给用户

---

## 三、架构 / 数据错误

### 13. 没有提前定义统一的设计参数
- **何时**：全程
- **错误**：字号、圆角、颜色、间距在多个地方各自定义，不一致
- **根因**：没有一开始就建 `design-standards.md`
- **正确做法**：开局先建设计规范文件，所有人统一引用

### 14. 回调里反复 findViewById 导致空指针崩溃
- **何时**：2026-06-21
- **错误**：`onIndexChanged` 回调每次都 `findViewById(R.id.tvUnitHint)`，快速滑动时返回 null → 崩溃
- **根因**：高频回调里不应该重复查找视图；Dialog 状态变化时 findViewById 可能返回 null
- **正确做法**：onCreate 时缓存引用到成员变量，回调里用 `?.` 安全调用

---

## 四、根本性错误（导致其他所有错误的根因）

### 错误是果，根因是因：

| 根因 | 导致的错误 |
|------|-----------|
| 没有设计规范就动手 | UI不一致、多次返工 |
| 改完不自查就推 | 编译报错、引用悬空 |
| 没有测试覆盖各版本 | Android 12+ NumberPicker 失效 |
| 不主动判断，推决定给用户 | 多轮往返、效率低 |
| 没有错误清单积累 | 同样的错重复犯 |

---

## 五、错误启发：开发前中后的检查机制

### 开发前（动手前）
- [ ] 想清楚改哪些文件，改动的逻辑是什么
- [ ] 相关联的文件都打开，全部看一遍
- [ ] 估计会不会影响其他功能

### 开发中（写代码时）
- [ ] 删除/重命名 → 立刻全局搜索这个名字的所有引用
- [ ] 新增 ID → 同时在 Kotlin 里绑定
- [ ] 新增依赖 → 先验证可用性

### 开发后（提交前）
- [ ] 自己先构建一次
- [ ] 自己判断 UI 是否协调（不要问用户）
- [ ] 跨文件交叉检查：改了这个文件，相关文件要改吗？
- [ ] 上方的"举一反三清单"过一遍

---

*最后更新：2026-06-21（从头到尾完整整理）*

---

## 追加：2026-07-01 构建 / 测试环境问题

### 15. 中文项目路径导致 Android Kotlin 单测运行器找不到测试类

- **何时**：2026-07-01，补 P1 单元测试时。
- **错误现象**：`compileDebugUnitTestKotlin` 成功，`AlarmTimeCalculatorTest.class` 已生成，`javap` 也能读取；但 `testDebugUnitTest` 报 `ClassNotFoundException: com.cyclealarm.app.domain.AlarmTimeCalculatorTest`。
- **根因**：当前项目路径包含中文字符，Gradle/AGP 生成的 test worker classpath 在本地 daemon 中出现路径编码错乱，导致 Java 测试进程实际加载不到项目内 class。
- **修复**：在 `app/build.gradle.kts` 中，测试前把 debug main/test Kotlin class 同步到用户 `.gradle` 下的 ASCII 路径，并在 `testDebugUnitTest` 执行前加入该路径到 classpath。
- **正确做法**：保留该测试 classpath 兼容配置；如果以后把项目迁移到纯 ASCII 路径，先确认 `testDebugUnitTest` 原生通过，再考虑移除兼容配置。

### 16. 误把备份 PNG 放进 Android `res` 目录导致资源合并失败

- **何时**：2026-07-03，新版 Pixso 风格 APK 图标和页面细节优化后重新构建。
- **错误现象**：一开始怀疑是 Gradle 缓存被清理；联网构建恢复插件解析后，`mergeDebugResources` 失败，报错 `app_icon_generated.backup.png` 文件名非法。
- **根因**：Android 会把 `app/src/main/res/**` 下的文件全部当作资源处理。资源文件名只能包含小写字母、数字和下划线；`app_icon_generated.backup.png` 中间多了一个点号，且备份文件本就不应该放在 `res` 目录。
- **修复**：把备份图标移出资源目录，放到 `backups/icons/app_icon_generated_backup.png`；`res/mipmap-*` 中只保留真正参与打包的合法资源文件。
- **正确做法**：任何备份文件、参考图、临时图都不要放进 `app/src/main/res`；需要备份统一放到 `backups/`、`design/` 或仓库外部。新增资源文件前检查命名规则：`^[a-z0-9_]+\\.(png|xml|webp|jpg)$`。
