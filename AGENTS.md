# Codex 项目约定

## 当前项目状态

本仓库已经是可构建的 Android 原生工程，用于继续制作手机周期闹钟软件。当前主线只做第一个功能“每 N 天闹钟”的稳定闭环；其他 5 个功能继续后置，不阻塞第一功能上线。

当前已迁入并保留：

- 每 N 天闹钟的创建、编辑、列表、启停、删除基础流程。
- 系统闹钟注册、触发、贪睡、关闭后重排。
- 开机、系统时间变化、时区变化、应用更新后的重排。
- 响铃前台服务、全屏响铃页、高优先级通知。
- `必要权限` 页面和 10 秒测试响铃。
- 从旧功能线程迁入的权限与响铃可靠性工作，详见 `PERMISSION_HANDOFF.md`。

不要继续在归档的 `功能2：每N小时闹钟` 线程主线开发；如需参考，只把它当作来源档案。

## 首要目标

- Android 优先。
- 中国安卓应用市场首发。
- 最低支持 Android 5.0 / API 21。
- 旧机、低端机、国产 ROM 兼容优先。
- 闹钟可靠性优先于视觉复杂度。
- 基础功能离线可用，不强制登录。

## 工作原则

- 第一轮只做“每 N 天闹钟”闭环。
- 其他 5 项功能后置，不抢主线资源。
- 不引入广告 SDK，除非用户明确要求。
- 不引入云同步、账号系统或后端服务，除非用户明确要求。
- 不申请当前功能不需要的权限。
- 界面默认极简、舒适、低操作负担：清晰层级、留白、克制颜色、少步骤。
- 大 UI 改动必须保留闹钟可靠性链路：调度、通知、响铃页、前台服务、重启重排、权限页。

## 新会话启动顺序

新 Codex 会话或新 agent 接手时，先读：

1. `CONTEXT_INDEX.md`
2. `TASK_ROUTING.md`
3. 与任务匹配的 `.agents/*.md`
4. `CURRENT_STATUS.md`
5. 修改前读 `RULES_BEFORE_EDIT.md`
6. 修改后按 `RULES_AFTER_EDIT.md` 检查

发布、权限、上架相关任务还必须读 `RULES_RELEASE.md`、`PERMISSION_HANDOFF.md`、`PERMISSION_REVIEW.md`、`STORE_READY.md`。

## 核心文档

- `CONTEXT_INDEX.md`: 新会话上下文入口。
- `TASK_ROUTING.md`: 任务路由和必读资料。
- `CURRENT_STATUS.md`: 当前状态、最近完成、下一步。
- `PROJECT_RULES.md`: 项目通用规则和历史坑。
- `BUILD_AND_RUN.md`: 构建、运行、调试说明。
- `PERMISSION_HANDOFF.md`: 权限和响铃可靠性交接。
- `PRD.md`: 产品需求。
- `TECH_PLAN.md`: 技术方案。
- `TEST_PLAN.md`: 测试计划。
- `STORE_READY.md`: 上架和盈利准备。
- `_MISTAKES.md`: 错误账本。

## 技术方向

- Android 原生。
- 轻量原生 UI。
- `minSdkVersion` 保持 21。
- `targetSdkVersion` 按目标应用商店当期要求设置。
- 本地存储优先本地文件、SQLite 或 Room；第一版不依赖网络。
- 周期计算逻辑必须放在可单元测试的 domain 层。
- 系统闹钟注册逻辑集中在 scheduler 层。

## 权限和合规

只申请当前功能需要的权限。

禁止为首版基础功能申请：

- 通讯录
- 短信
- 通话记录
- 相机
- 麦克风
- 全部文件访问

定位权限只允许地点触发闹钟使用。用户拒绝定位时，其他闹钟功能必须可用。

精准闹钟、通知、全屏提醒、悬浮窗必须有清楚的产品用途和商店审核说明。

## 测试要求

重要代码改动必须考虑：

- Android 5.0 兼容。
- 锁屏触发。
- App 后台触发。
- 重启后重排。
- 系统时间变化。
- 时区变化。
- 权限拒绝。
- 国产 ROM 后台限制。

周期计算逻辑必须有单元测试或等价测试用例。改周期规则时，必须同步更新测试。

## PDCA 固定流程

- Plan: 明确任务目标、影响范围、相关文件、验证方式。
- Do: 按任务路由修改，避免无关重构。
- Check: 运行对应测试或构建，检查引用、权限、文档一致性。
- Act: 阶段完成后更新 `CURRENT_STATUS.md`；发现新错误写入 `_MISTAKES.md`；可能复发的问题升级到规则文件。

## Codex 操作约束

- 修改代码前先读现有文件。
- 不删除用户或 Qclaw 已有文件，除非用户明确要求。
- 不运行破坏性 Git 命令。
- 不把密钥、签名证书、账号密码写入仓库。
- 手动文件编辑使用 `apply_patch`。
