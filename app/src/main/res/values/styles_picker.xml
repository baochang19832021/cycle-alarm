<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="IntervalPickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowAnimationStyle">@style/PickerDialogAnimation</item>
    </style>

    <style name="PickerDialogAnimation" parent="android:Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

    <!-- NumberPicker text styling to match Xiaomi look -->
    <style name="CustomNumberPickerTheme" parent="@android:style/Widget.Material.NumberPicker">
        <item name="android:textSize">36sp</item>
        <item name="android:textColorPrimary">#FF1A1A1A</item>
        <item name="android:textColorSecondary">#FFCCCCCC</item>
    </style>
</resources>
