# 手机周期闹钟 App 测试计划

## 1. 测试目标

测试目标不是只证明功能能点通，而是证明闹钟在真实手机环境下尽力可靠。当前第一轮只测试“每 N 天闹钟”的完整闭环，重点覆盖旧系统、低端机、国产 ROM、锁屏、重启、低电量、系统时间变化和权限被拒绝等情况。

默认最低系统：Android 5.0 / API 21。

## 2. 系统版本矩阵

| 系统版本 | API | 优先级 | 重点 |
| --- | --- | --- | --- |
| Android 5.0-5.1 | 21-22 | P0 | 锁屏触发、重启恢复、`setExact` 调度 |
| Android 6.0 | 23 | P0 | Doze、锁屏触发、重启恢复 |
| Android 7.x | 24-25 | P0 | 旧机通知、定时兼容、低内存 |
| Android 8.x | 26-27 | P0 | 通知渠道、后台限制 |
| Android 9 | 28 | P1 | 省电策略、后台行为 |
| Android 10 | 29 | P1 | 后台定位边界、国产 ROM 兼容 |
| Android 11 | 30 | P1 | 存储限制、通知行为 |
| Android 12 | 31-32 | P0 | 精准闹钟特殊权限 |
| Android 13 | 33 | P0 | 通知运行时权限、精准闹钟权限 |
| Android 14+ | 34+ | P0 | 全屏提醒权限、最新后台限制 |

P0 表示发布前必须覆盖。P1 表示发布前尽量覆盖，若缺真机则用模拟器补齐。

## 3. 真机矩阵

至少准备：

- 1 台 Android 5-7 老机。
- 1 台 Android 8-10 低端机。
- 1 台小米/红米或 HyperOS/MIUI 设备。
- 1 台 OPPO、vivo、荣耀或华为设备。

模拟器负责补齐系统版本；真机负责验证锁屏、响铃、重启、后台、省电和厂商权限管理。

每台真机记录：

- 品牌
- 型号
- Android 版本
- ROM 版本
- 内存
- 是否开启省电模式
- 是否允许自启动
- 是否允许后台运行
- 是否允许通知
- 是否允许精准闹钟
- 是否允许全屏提醒

## 4. 第一轮基础功能测试

### 4.1 创建闹钟

- 创建每 N 天闹钟。
- 点击其他 5 项入口时，如功能未开放，应显示明确的“后续支持”，且不申请无关权限。

验收：

- 表单校验明确。
- 保存后列表出现新闹钟。
- 下次响铃时间正确。
- 关闭开关后不再触发。
- 删除后取消系统闹钟。

### 4.2 编辑和删除

- 编辑名称。
- 编辑时间。
- 编辑周期参数。
- 开关启用/停用。
- 删除闹钟。

验收：

- 编辑后旧系统闹钟被取消，新系统闹钟被注册。
- 删除后不会再次响铃。
- 列表刷新正确。

## 5. 第一轮周期计算用例

### 5.1 每 N 天

- 起始日期为今天，时间在未来。
- 起始日期为今天，时间已过去。
- 起始日期为 30 天前。
- N = 1。
- N = 2。
- N = 999。
- 跨月。
- 跨年。
- 闰年 2 月 29 日附近。

### 5.2 后续功能占位测试

第一轮不实现以下功能，只检查入口状态：

- 每 N 小时入口不崩溃。
- 每月 X 号入口不崩溃。
- 农历日期入口不崩溃。
- 法定假日调休入口不崩溃。
- 地点触发入口不崩溃，且不提前申请定位权限。

## 6. 后续周期计算用例

### 6.1 每 N 小时

- 从当前时间后 5 分钟开始。
- N = 1。
- N = 6。
- N = 24。
- 设置有效时间段 08:00-22:00。
- 当前周期点落在有效时间段外。
- 跨天。

### 6.2 每月 X 号

- X = 1。
- X = 28。
- X = 29。
- X = 30。
- X = 31。
- 当前月份不存在 X 号时跳过。
- 当前月份存在 X 号但时间已过时进入下一个月。

默认规则：当月无该日则跳过该月，不自动改为月末。

### 6.3 农历日期

- 正月初一。
- 八月十五。
- 腊月三十。
- 小月没有三十时的处理。
- 闰月年份。
- 默认不包含闰月同日。
- 用户选择闰月模式后的下一次触发。

### 6.4 法定假日调休

- 普通工作日。
- 普通周末。
- 法定节假日。
- 调休补班日。
- 节假日前一天。
- 年度数据缺失时的提示。

### 6.5 地点触发

后置测试：

- 用户拒绝定位权限。
- 用户只给前台定位。
- 用户给后台定位。
- 进入围栏触发。
- 离开围栏触发。
- 半径过小提示。
- 位置服务关闭提示。

## 7. 系统场景测试

### 6.1 锁屏和后台

- App 在前台，等待闹钟触发。
- App 在后台，等待闹钟触发。
- App 被划掉，等待闹钟触发。
- 锁屏 10 分钟后触发。
- 锁屏 2 小时后触发。
- 夜间长时间待机后触发。

验收：

- 触发时能提醒用户。
- 可关闭。
- 可贪睡。
- 关闭后会注册下一次。

### 6.2 重启和系统变化

- 创建闹钟后重启手机。
- 重启后不打开 App，等待闹钟触发。
- 手动修改系统时间。
- 手动修改时区。
- App 升级覆盖安装。
- 清除 App 数据。

验收：

- 重启和升级后启用闹钟被重新计算。
- 清除数据后不再响旧闹钟。
- 时间和时区变化后下次触发时间合理。

### 6.3 电量和系统模式

- 低电量模式。
- 省电模式。
- 超级省电模式。
- 勿扰模式。
- 静音模式。
- 音量为 0。

验收：

- App 对受系统限制的场景有说明。
- 不承诺绕过系统策略。
- 必要时引导用户开启系统闹钟、通知、自启动、后台运行等设置。

## 8. 权限测试

通知权限：

- 首次创建闹钟时申请。
- 用户拒绝。
- 用户允许。
- 用户后续在系统设置关闭。

精准闹钟：

- Android 12+ 检查权限。
- 权限被拒绝时展示设置入口。
- 权限被撤销后重排失败并提示。

全屏提醒：

- Android 14+ 检查权限。
- 不允许时退化为高优先级通知。
- 只在响铃页使用，不用于营销或普通提示。

定位权限：

- 只在创建地点触发闹钟时申请。
- 拒绝后不影响其他功能。

## 9. 性能测试

低端机指标：

- 冷启动尽量控制在 2 秒内。
- 首页列表滚动不卡顿。
- 100 条闹钟规则下仍可打开列表和编辑。
- 响铃页启动不依赖网络。
- 不在后台长期占用 CPU。

包体要求：

- 首版避免接入广告 SDK。
- 首版避免重型分析 SDK。
- 图片资源压缩。
- 节假日 JSON 按年份拆分。

## 10. 上架前回归清单

- 全部 P0 系统版本通过。
- 至少 4 台真机通过核心闹钟场景。
- 权限拒绝路径通过。
- 隐私政策与实际权限一致。
- 商店截图与实际功能一致。
- 没有隐藏功能、测试按钮、占位文字。
- 付费功能如未实现，不在商店文案中承诺。
- 地点触发若未完成，不放入首版商店主宣传。
