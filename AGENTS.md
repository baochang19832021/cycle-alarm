# Codex 项目约定

## 项目背景

本仓库用于继续制作 Qclaw 未完成的手机周期闹钟软件。当前阶段优先补齐产品、技术、测试和上架文档；Qclaw 原型代码尚未导入。用户已经明确：导入 Qclaw 源码后，先实现第一个功能“每 N 天闹钟”，其他 5 个功能先不急着实现。

目标平台：

- Android 优先。
- 中国安卓应用市场首发。
- 最低支持 Android 5.0 / API 21。
- 旧机、低端机、国产 ROM 兼容优先。

## 工作原则

- 未导入 Qclaw 项目前，不假设已有代码结构。
- 接管 Qclaw 后，先审计、构建、运行，再改代码。
- 闹钟可靠性优先于视觉复杂度。
- 基础功能离线可用，不强制登录。
- 不引入广告 SDK，除非用户明确要求。
- 不引入云同步、账号系统或后端服务，除非用户明确要求。
- 第一轮只做“每 N 天闹钟”闭环。
- 其他 5 项功能后置，不阻塞第一功能上线。
- 界面默认极简、舒适、低操作负担：优先清晰层级、留白、克制颜色和少步骤，不为装饰或复杂度增加控件。

## 文档

核心文档：

- `PRD.md`: 产品需求。
- `TECH_PLAN.md`: 技术方案。
- `TEST_PLAN.md`: 测试计划。
- `STORE_READY.md`: 上架和盈利准备。

导入 Qclaw 项目后，新增：

- `QCLAW_AUDIT.md`: 原型接管审计。
- `BUILD_AND_RUN.md`: 构建、运行、调试说明。

## 预期技术方向

如果需要新建或重构 Android 工程：

- 优先 Android 原生。
- 优先轻量原生 UI。
- `minSdkVersion` 默认 21。
- `targetSdkVersion` 按目标应用商店当期要求设置。
- 本地存储优先 Room 或 SQLite。
- 周期计算逻辑放在可单元测试的 domain 层。
- 系统闹钟注册逻辑集中在 scheduler 层。

## 权限和合规

只申请当前功能需要的权限。

禁止为首版基础功能申请：

- 通讯录
- 短信
- 通话记录
- 相机
- 麦克风
- 全部文件访问

定位权限只允许地点触发闹钟使用。用户拒绝定位时，其他闹钟功能必须可用。

精准闹钟、通知、全屏提醒必须有清楚的产品用途和商店审核说明。

## 测试要求

重要代码改动必须考虑：

- Android 5.0 兼容。
- 锁屏触发。
- App 后台触发。
- 重启后重排。
- 系统时间变化。
- 时区变化。
- 权限拒绝。
- 国产 ROM 后台限制。

周期计算逻辑必须有单元测试或等价测试用例。

## Codex 操作约束

- 修改代码前先读现有文件。
- 不删除用户或 Qclaw 已有文件，除非用户明确要求。
- 不运行破坏性 Git 命令。
- 不把密钥、签名证书、账号密码写入仓库。
- 手动文件编辑使用 `apply_patch`。
